# 使用演示

## 界面预览

### 主界面 - MCP 服务器列表

```
╔══════════════════════════════════════════════════════════════╗

   🚀 MCP Skills Manager - Claude Code   

╚══════════════════════════════════════════════════════════════╝

[1. MCP 服务器]   2. Skills 

► 🟢 github (http) [已启用]
  🟢 PP-StructureV3 (uvx) [已启用]
  🟢 PaddleOCR-VL (uvx) [已启用]
  🟢 chrome-devtools (npx) [已启用]
  🟢 context7 (npx) [已启用]
  🟢 fetch (uvx) [已启用]
  🟢 filesystem (npx) [已启用]
  🟢 mcp-code-executor (node) [已启用]
  ...

┌────────────────────────────────────────────────────────────┐
│ 快捷键: [↑↓] 选择 | [Enter/Space] 切换启用 | [d] 删除    │
│         [r] 刷新 | [Tab/1/2] 切换标签 | [q] 退出         │
└────────────────────────────────────────────────────────────┘

MCP 服务器: 22 个 (22 已启用) | Skills: 0 个
```

## 操作演示

### 1. 启用/禁用 MCP 服务器

**操作前：**
```
► 🟢 puppeteer (npx) [已启用]
```

**按下 Enter 键后：**
```
✅ puppeteer 已禁用

► ⚪ puppeteer (npx) [已禁用]
```

**再次按下 Enter 键：**
```
✅ puppeteer 已启用

► 🟢 puppeteer (npx) [已启用]
```

### 2. 删除 MCP 服务器

**操作前：**
```
  🟢 github (http) [已启用]
► 🟢 chrome-devtools (npx) [已启用]
  🟢 context7 (npx) [已启用]
```

**按下 'd' 键后：**
```
✅ 已删除 chrome-devtools

  🟢 github (http) [已启用]
► 🟢 context7 (npx) [已启用]
```

### 3. 切换标签

**MCP 服务器标签（默认）：**
```
[1. MCP 服务器]   2. Skills 
```

**按下 '2' 或 Tab 键后 - Skills 标签：**
```
 1. MCP 服务器  [2. Skills]

暂无 Skills (功能开发中)
```

### 4. 错误处理

**当操作失败时：**
```
❌ 错误: Failed to save config: Permission denied
```

### 5. 刷新配置

**按下 'r' 键：**
```
✅ 已刷新配置
```

## 状态指示说明

| 图标 | 状态 | 说明 |
|------|------|------|
| 🟢 | 已启用 | MCP 服务器当前启用，Claude Code 会加载 |
| ⚪ | 已禁用 | MCP 服务器被禁用，配置中有 `disabled: true` |
| ► | 选中 | 当前选中的项目，可以进行操作 |

## 配置文件变化

### 禁用服务器前的配置

```json
{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"]
    }
  }
}
```

### 禁用服务器后的配置

```json
{
  "mcpServers": {
    "puppeteer": {
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-puppeteer"],
      "disabled": true
    }
  }
}
```

### 删除服务器后的配置

```json
{
  "mcpServers": {
    // puppeteer 配置已被移除
  }
}
```

## 备份文件示例

在 `~/.claude-backups/` 目录中：

```
claude-2026-02-21T08-15-30-123Z.json  (最新)
claude-2026-02-21T08-10-15-456Z.json
claude-2026-02-21T08-05-00-789Z.json
claude-2026-02-20T23-50-45-012Z.json
...
```

每次修改配置前都会自动创建备份，保留最近 10 个。

## 使用技巧

1. **快速导航**: 使用 `↑` `↓` 键快速浏览列表
2. **批量操作**: 目前需要一个个操作，批量功能正在开发中
3. **安全第一**: 所有修改都会自动备份，不用担心误操作
4. **实时同步**: 修改立即保存到 `~/.claude.json`，重启 Claude Code 即可生效
5. **状态检查**: 底部统计信息显示当前启用的服务器数量

## 常见使用场景

### 调试时临时禁用某些服务器
```
1. 启动 skills-manager
2. 禁用可能有问题的服务器
3. 退出并重启 Claude Code 测试
4. 如果问题解决，说明该服务器有问题
5. 根据需要重新启用或删除
```

### 管理大量 MCP 服务器
```
1. 按需启用/禁用服务器
2. 删除不再使用的服务器
3. 保持配置文件整洁
4. 定期检查备份文件
```

### 快速恢复到之前的配置
```
1. 查看 ~/.claude-backups/ 找到需要的备份
2. 复制备份文件覆盖 ~/.claude.json
3. 在 skills-manager 中按 'r' 刷新
```
